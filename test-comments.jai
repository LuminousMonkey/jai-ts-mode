// This is a single-line comment that should wrap properly
// This is part of the same paragraph, since it's on the adjacent line

// This is a new paragraph because of the blank line above

// Multiple consecutive lines
// should be treated as a single asldkjf;asljdf sdkjf;sadkfj ;sa djf ;lsjdf ;lsjdf ;lsf ;
// paragraph for wrapping

/* This is a block comment
   that should wrap properly
   when I run M-q */

    // Indented comments should
    // maintain their indentation
    // when wrapped

    /* Indented block comments
       should also maintain their
       indentation when wrapped */

    // This comment and
    // the next one should be in separate paragraphs

    // Because of the blank line between them